export const styles = {
  // Screen reader only (visually hidden)
  srOnly: 'sr-only',

  // Visual toast notification
  toast: 'fixed bottom-4 right-4 bg-zinc-800 border border-zinc-700 text-zinc-100 rounded-lg shadow-lg px-4 py-3 animate-slide-up',
}
